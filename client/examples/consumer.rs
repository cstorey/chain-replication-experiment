fn main() {
    /*
    let mut producer = Producer::new("head:7200");
    let mut consumer = Consumer::new("tail:7200");

    let fut = producer.publish("blah!");
    let resp = fut.await();
    
    producer.read_from(resp.seqno);
    
    */
}
